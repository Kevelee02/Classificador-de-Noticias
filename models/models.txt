Modelo_1 com 0.75 de acuracia = SGDClassifier(
        loss="log_loss",
        class_weight="balanced",
        max_iter=1000,
        n_jobs=-1,             
        random_state=42,
        early_stopping=True,    
        n_iter_no_change=5,
        verbose=1
    )

Modelo_2 com 0.84 de acuracia: SGDClassifier(
    loss="log_loss",
    class_weight="balanced",
    max_iter=2000,              # já que sobra tempo agora, dá pra deixar rodar mais
    alpha=1e-5,                 # menos regularização — testar também 1e-6, 1e-4
    early_stopping=False,       # usa todos os dados de treino, sem reservar validação interna
    n_jobs=-1,
    random_state=42
)

Modelo_3 com 0.86 de acuracia: LinearSVC(
      C=1.0,                  
    loss="squared_hinge",  
    dual=False,            
    class_weight="balanced",
    max_iter=5000,        
    tol=1e-3,
    random_state=42
)